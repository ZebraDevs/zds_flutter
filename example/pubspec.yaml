name: zds_flutter_example

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  lottie: ^3.3.1
  scrollable_positioned_list: ^0.3.8
  zds_flutter:
    path: ../

dependency_overrides:
  intl: ^0.20.2

flutter:
  uses-material-design: true
  assets:
    - assets/colors.json
    - assets/notification.wav
